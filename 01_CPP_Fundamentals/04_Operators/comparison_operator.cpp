#include <iostream>

int main()
{
    /**COMPARISON OPERATOR
     * == (Equal to)
     * != (Not equal to)
     * <= (Less than or equal to)
     * >= (Greater than or equal to)
     * < (Less than)
     * > (Greater than)
     */

    // Comparision operator retourn true or false

    std::cout << (10 > 2);  // 1 => true
    std::cout << (10 == 2); // 0 => false

    return 0;
}